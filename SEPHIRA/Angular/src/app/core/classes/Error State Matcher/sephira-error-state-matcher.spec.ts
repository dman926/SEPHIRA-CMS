import { SephiraErrorStateMatcher } from './sephira-error-state-matcher';

describe('ErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new SephiraErrorStateMatcher()).toBeTruthy();
  });
});
